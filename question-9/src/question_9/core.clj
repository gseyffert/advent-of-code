(ns question-9.core
  (:gen-class))

(defn decompress-chunk [chunk] nil)

(defn parse-compression-direction [dir] nil)

(defn solve-part-1 [compressed] nil)

(defn solve-part-2 [compressed] nil)

(defn solve-question [compressed]
  {:part-1 (solve-part-1 compressed)
   :part-2 (solve-part-2 compressed)})
